---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 6.10.0

inputs:
  - name: dp-dd-react-app

outputs:
  - name: dist

run:
  path: dp-dd-react-app/ci/scripts/npm-install.sh
